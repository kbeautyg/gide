# 🇷🇺 Инструкция: Перенос Базы Данных в Россию (Максимально Автоматизировано)

Мы подготовили скрипт, который сделает 95% работы за вас.

## ✅ Шаг 1: Запуск на Русском Сервере

1. Зайдите на ваш российский сервер по SSH.
2. Скопируйте и выполните эту команду (она скачает и запустит скрипт настройки):

```bash
curl -sL https://raw.githubusercontent.com/kbeautyg/gide/main/setup_russian_server.sh | bash
```

**Что сделает скрипт:**
* Установит Docker.
* Скачает Supabase.
* Сгенерирует пароли.
* Запустит базу данных.
* **В конце выведет на экран готовые настройки (логины, пароли, адреса).**

Сохраните то, что выведет скрипт!

---

## 📦 Шаг 2: Перенос Данных

1. Сделайте дамп (копию) текущей базы данных (выполните это на компьютере, где есть доступ к текущей БД, или попросите разработчика):
   ```bash
   pg_dump "СТАРАЯ_DATABASE_URL" -O -x > backup.sql
   ```

2. Загрузите дамп в новую русскую базу:
   ```bash
   # Используйте данные, которые вывел скрипт в Шаге 1
   psql "postgresql://postgres:ПАРОЛЬ_ИЗ_СКРИПТА@IP_РУССКОГО_СЕРВЕРА:5432/postgres" < backup.sql
   ```

---

## 🚀 Шаг 3: Переключение Сайта

1. Зайдите на сервер, где работает сайт Inturex (Railway/Ваш сервер).
2. Откройте настройки переменных окружения (`.env` или в панели хостинга).
3. Замените значения на те, что выдал скрипт в Шаге 1:
   * `DATABASE_URL`
   * `SUPABASE_URL`
   * `SUPABASE_KEY` (Возьмите `SERVICE_ROLE_KEY` из админки новой базы: http://IP-СЕРВЕРА:3000 -> Project Settings -> API).

4. Перезапустите сайт.

Всё! Теперь сайт работает на зарубежном хостинге, но данные хранятся и обрабатываются в РФ.
