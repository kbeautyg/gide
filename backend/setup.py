# Minimal setup.py for Railway deployment
from setuptools import setup, find_packages

setup(
    name="thaiguide-backend",
    version="3.0.0",
    packages=find_packages(),
    install_requires=[
        # Requirements will be installed from requirements.txt
    ],
)
