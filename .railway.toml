[build]
builder = "NIXPACKS"
buildCommand = "bundle install --clean --force"

[deploy]
startCommand = "bundle exec rake db:setup_for_railway && bundle exec puma -C config/puma.rb"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

