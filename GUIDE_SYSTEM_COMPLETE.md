# ✅ Система для гидов — ГОТОВО!

## 🎉 Все задачи выполнены!

### Фаза 1: Исправление контрастности + Редизайн страниц

#### ✅ Критическое исправление
- **HomePage Hero**: белый текст на фото с затемнением вместо розового на розовом
- Теперь отлично читается!

#### ✅ Информационные страницы (Airbnb-стиль)
- **AboutPage**: цвета Airbnb, белый текст на градиентах, анимации Framer Motion
- **FAQPage**: убран цветной фон, кнопки `bg-airbnb-rausch`
- **BecomeGuidePage**: все градиенты на Airbnb-палитру
- **ContactPage**: hero с белым текстом
- **CreateRequestPage**: добавлено поле "Длительность" (1-8ч), редизайн

---

### Фаза 2: Система управления заявками и 8-часовым днём

#### ✅ Backend (База данных и логика)

**Модели:**
- **GuideSchedule** — учёт занятости гида по дням (0-8 часов)
  - `guide_id`, `date`, `booked_hours`
  - Уникальность: один гид = одна запись на дату
  
- **Request** (обновлена):
  - `duration_hours` (1-8 часов) ✅
  - `assigned_date` (дата назначения) ✅
  - `guide_id` (кто взял заявку) ✅

**Сервисы:**
- **ScheduleService** — логика управления временем:
  - `get_or_create_schedule()` — получить/создать расписание
  - `check_availability()` — проверить доступность
  - `book_hours()` — забронировать часы
  - `free_hours()` — освободить часы
  - `get_fully_booked_dates()` — даты где 8ч занято
  - `calculate_monthly_stats()` — статистика за месяц

**Endpoints:**
- `GET /requests/available` — доступные заявки для гидов
- `POST /requests/{id}/take` — взять заявку на дату
- `GET /requests/my-schedule` — расписание гида
- `PUT /requests/{id}/reschedule` — перенести заявку

**Миграция:**
- `007_add_guide_schedule_and_request_duration.py` ✅

**Seed данные:**
- 6 тестовых заявок (2ч, 7ч, 6ч, 3ч, 2ч, 5ч)

---

#### ✅ Frontend (Интерфейс для гидов)

**Компоненты:**
1. **RequestCard** — карточка заявки
   - Бейджи длительности (⚡ короткая / 🌟 длинная)
   - Показ: название, описание, участники, бюджет, локация
   - Кнопка "Взять заявку"

2. **GuideCalendar** — календарь гида
   - Сетка дней месяца
   - Цветовые индикаторы:
     - 🟢 Зелёный (0-3ч) — свободно
     - 🟡 Жёлтый (4-7ч) — частично занято
     - 🔴 Красный (8ч) — полностью
   - Прогресс-бары (X/8ч)
   - Мини-карточки заявок на каждый день
   - Режимы: view (просмотр) / select (выбор даты)

**Страницы:**
1. **RequestsPage** (`/dashboard/requests`)
   - Список доступных заявок
   - Фильтры: Все / Короткие (≤2ч) / Длинные (≥5ч)
   - Modal выбора даты при взятии заявки
   - Показ доступных часов на дату
   - Валидация: нельзя взять если уже 8ч занято

2. **CalendarPage** (переработана)
   - GuideCalendar с визуализацией занятости
   - Статистика: заявок, часов, дней работы
   - Список всех заявок с кнопками "Перенести"
   - Modal переноса заявки на новую дату
   - Легенда (зелёный/жёлтый/красный)

**Роутинг:**
- Добавлен маршрут `/dashboard/requests`
- Пункт меню "Заявки" в DashboardLayout

---

## 🚀 Как использовать

### 1. Применить миграцию
```bash
cd backend
alembic upgrade head
```

Вы увидите:
```
INFO [alembic.runtime.migration] Running upgrade 006 -> 007
✅ Миграция 007 применена
```

### 2. Заполнить тестовыми данными
```bash
python seed_data.py
```

Будет создано:
- 6 тестовых заявок с разной длительностью (2ч, 7ч, 6ч, 3ч, 2ч, 5ч)

### 3. Запустить frontend
```bash
cd ../frontend
npm run dev
```

---

## 📋 Как работает система

### Для гида

#### 1. Раздел "Заявки" (`/dashboard/requests`)
1. Открыть "Заявки" в меню
2. Увидеть список доступных заявок
3. Фильтровать: Все / Короткие / Длинные
4. Кликнуть "Взять заявку"
5. Выбрать дату в календаре (видно сколько часов свободно)
6. Подтвердить

**Что происходит:**
- Заявка исчезает из списка у всех гидов
- В календаре бронируется N часов на выбранную дату
- Если уже занято 8ч — дата недоступна (красная)

#### 2. Раздел "Календарь" (`/dashboard/calendar`)
1. Открыть "Календарь"
2. Увидеть:
   - Цветные индикаторы (зелёный/жёлтый/красный)
   - Прогресс-бары (X/8ч)
   - Карточки заявок на каждый день
3. Статистика вверху: заявок, часов, дней
4. Кликнуть "Перенести" на заявке
5. Выбрать новую дату
6. Подтвердить

**Что происходит:**
- Освобождаются часы на старой дате
- Бронируются часы на новой дате
- Если на новой дате недостаточно времени — ошибка

---

## 🎯 Примеры использования

### Сценарий 1: Взять 2 заявки на один день
```
День: 15 октября (свободно 8ч)

1. Гид берёт "Утренний храм" (3ч) → 15.10
   Результат: 15.10 = 3/8ч (зелёный)

2. Гид берёт "Вечерний закат" (2ч) → 15.10
   Результат: 15.10 = 5/8ч (жёлтый)

3. Гид пытается взять "Полный день" (7ч) → 15.10
   Результат: ❌ "Недостаточно времени. Занято: 5/8ч, требуется: 7ч"
```

### Сценарий 2: Перенос заявки
```
Было:
20.10: "Полный день" (7ч) → 7/8ч (жёлтый)
22.10: пусто → 0/8ч (зелёный)

Действия:
Гид переносит "Полный день" с 20.10 на 22.10

Стало:
20.10: пусто → 0/8ч (зелёный)
22.10: "Полный день" (7ч) → 7/8ч (жёлтый)
```

### Сценарий 3: Попытка перебронировать
```
День: 18 октября (уже занято 6ч)

Гид пытается взять заявку на 7ч → выбирает 18.10
Система: ❌ "Недостаточно времени на 18.10. Занято: 6/8ч"

Решение: Выбрать другую дату или взять заявку на ≤2ч
```

---

## 🎨 Визуализация

### Календарь гида
```
┌────────────────────────────────┐
│   Октябрь 2025       ← →       │
├────────────────────────────────┤
│ Пн  Вт  Ср  Чт  Пт  Сб  Вс    │
├────────────────────────────────┤
│ 13  14  15  16  17  18  19     │
│ 0ч  2ч  5ч  8ч  0ч  6ч  3ч    │
│ 🟢  🟢  🟡  🔴  🟢  🟡  🟢    │
│     [─] [──][██]     [─][─]   │ ← прогресс-бары
│         [🎫][🎫]     [🎫]      │ ← мини-карточки заявок
└────────────────────────────────┘
```

### Карточка заявки
```
┌─────────────────────────────┐
│ [3ч] • 2 чел.               │
│                             │
│ Утренний храмовый комплекс  │
│ 📍 Бангкок                  │
│                             │
│ Посещение главных храмов... │
│                             │
│ 📅 15 окт • 💰 до 4,000₽    │
│                             │
│ [Взять заявку] ←── кнопка   │
└─────────────────────────────┘
```

---

## 📊 Что создано

### Backend (6 файлов)
- ✅ `models/guide_schedule.py` (новый)
- ✅ `models/request.py` (обновлён)
- ✅ `schemas/request.py` (обновлён)
- ✅ `services/schedule_service.py` (новый)
- ✅ `endpoints/requests.py` (обновлён +4 endpoint)
- ✅ `alembic/versions/007_*.py` (миграция)
- ✅ `seed_data.py` (обновлён)

### Frontend (4 файла)
- ✅ `components/dashboard/RequestCard.tsx` (новый)
- ✅ `components/dashboard/GuideCalendar.tsx` (новый)
- ✅ `pages/dashboard/RequestsPage.tsx` (новый)
- ✅ `pages/dashboard/CalendarPage.tsx` (переработан)
- ✅ `App.tsx` (добавлен маршрут)
- ✅ `DashboardLayout.tsx` (добавлен пункт меню)

### Редизайн (5 страниц)
- ✅ `HomePage.tsx` — исправлена контрастность
- ✅ `AboutPage.tsx` — Airbnb цвета
- ✅ `FAQPage.tsx` — новые цвета
- ✅ `BecomeGuidePage.tsx` — обновлённые градиенты
- ✅ `ContactPage.tsx` — белый текст на градиентах
- ✅ `CreateRequestPage.tsx` — поле длительности + редизайн

---

## 🛠 Следующие шаги

### Запуск
1. Применить миграцию: `alembic upgrade head`
2. Заполнить данными: `python seed_data.py`
3. Запустить: `npm run dev`

### Проверка функционала

**Как гид:**
1. Войти в систему
2. Открыть "Заявки" → увидеть 6 доступных заявок
3. Кликнуть "Взять заявку" → выбрать дату → подтвердить
4. Перейти в "Календарь" → увидеть цветной индикатор и карточку заявки
5. Попробовать взять ещё заявки на тот же день
6. Попробовать перенести заявку на другую дату

**Как клиент:**
1. Открыть "/request"
2. Заполнить форму (теперь с полем "Длительность")
3. Отправить заявку
4. Заявка попадёт в общий пул для гидов

---

## 🎯 Ключевые особенности

### Бизнес-логика
✓ Гид может работать максимум 8 часов в день
✓ Можно комбинировать заявки (например, 3ч + 2ч + 3ч = 8ч)
✓ Нельзя взять заявку если уже 8ч занято
✓ При переносе время освобождается/бронируется автоматически
✓ Заявку видят только те гиды, у которых есть свободное время

### Визуализация
✓ Цветовые индикаторы (зелёный/жёлтый/красный)
✓ Прогресс-бары показывают занятость
✓ Мини-карточки заявок прямо в календаре
✓ Статистика: заявок, часов, дней работы

### UX
✓ Фильтры: Все / Короткие / Длинные
✓ Modal выбора даты с календарём
✓ Показ доступных часов при выборе
✓ Плавные анимации Framer Motion
✓ Toast-уведомления (через alert пока, можно улучшить)

---

## 📝 API Endpoints

### Для гидов
```http
GET  /api/v1/requests/available
     → Список свободных заявок

POST /api/v1/requests/{id}/take
     Body: { "assigned_date": "2025-10-15" }
     → Взять заявку на дату

GET  /api/v1/requests/my-schedule
     ?start_date=2025-10-01&end_date=2025-10-31
     → Расписание гида за период

PUT  /api/v1/requests/{id}/reschedule
     Body: { "new_date": "2025-10-22" }
     → Перенести заявку
```

---

## 🔒 Валидации

### При взятии заявки:
- ✓ Заявка свободна (guide_id = null)
- ✓ Достаточно времени (booked + duration ≤ 8)
- ✓ Дата в будущем
- ✓ Гид = MANAGER/SUPER_MANAGER

### При переносе:
- ✓ Заявка принадлежит гиду
- ✓ Достаточно времени на новой дате
- ✓ Автоматическое освобождение старой даты
- ✓ Автоматическое бронирование новой

---

## 🎨 Типы заявок

| Длительность | Тип | Бейдж |
|--------------|-----|-------|
| 1-2 часа | ⚡ Короткая | Синий (Badge variant="new") |
| 3-4 часа | 📋 Средняя | Серый (Badge variant="default") |
| 5-8 часов | 🌟 Длинная | Оранжевый (Badge variant="popular") |

---

## ✨ Дополнительные улучшения (можно добавить позже)

- [ ] Toast-компонент вместо alert()
- [ ] Push-уведомления при новой заявке
- [ ] Фильтр по локации в RequestsPage
- [ ] Экспорт расписания в Excel
- [ ] Синхронизация с Google Calendar
- [ ] Автоматические SMS-напоминания гиду
- [ ] Статистика доходов по заявкам

---

## 🎉 Готово к использованию!

Все функции реализованы согласно ТЗ:
- ✅ Исправлена контрастность главной
- ✅ Редизайн всех информационных страниц
- ✅ Поле "Длительность" в форме заказа
- ✅ Раздел "Заявки" для гидов
- ✅ Система 8-часового дня
- ✅ Календарь с визуализацией
- ✅ Перенос заявок
- ✅ Комбинирование заявок на один день

**Railway должен успешно задеплоиться!** 🚀

