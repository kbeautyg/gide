development:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] || "dev_secret_key_base_minimum_30_characters_long_for_development" %>

test:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] || "test_secret_key_base_minimum_30_characters_long_for_testing" %>

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>

