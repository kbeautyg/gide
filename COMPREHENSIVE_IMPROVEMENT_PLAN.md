# 📋 ПОЛНЫЙ ПЛАН УЛУЧШЕНИЙ ThaiGuide Pro

**Дата:** 9 октября 2025, 14:00 MSK  
**На основе:** Детального изучения сайта через браузер + требования пользователя

---

## 🔍 ОБНАРУЖЕННЫЕ ПРОБЛЕМЫ

### 🔴 КРИТИЧЕСКИЕ:

1. **Страница /tours пустая (0 предложений)**
   - API возвращает 200, но фронтенд не отображает
   - Проблема в маппинге данных или фильтрации

2. **Секция "Популярные экскурсии" на главной пустая**
   - Туры есть в БД, но не загружаются

3. **Админ-панель не появляется в меню**
   - Роль определяется неправильно
   - Нужна проверка `/api/v1/users/me`

4. **Календарь: баг переноса даты (-1 день)**
   - Переносит на 22 вместо 23, на 29 вместо 30
   - Проблема в обработке timezone/Date

### 🟡 ВАЖНЫЕ:

5. **Название сайта "ThaiGuide Pro"**
   - Изменить на "Turex Pro"
   - Половина черная, половина малиновая (#FF385C)
   - Разделение по диагонали

6. **Поиск: слова сливаются с фоном**
   - "Экскурсии" и "Впечатления" нужны фоны
   - Убрать слово "НОВОЕ"
   - Убрать слово "Искать", оставить только лупу
   - Увеличить кнопку поиска (круглый пузырёк)

7. **Кнопка "Заказать экскурсию"**
   - Вынести в отдельный блок
   - Добавить sticky notification (снизу слева)
   - Закрывается по клику на крестик

8. **Фоновое фото Hero**
   - "Дешевизна" - убрать градиенты
   - Оставить только малиновый цвет (#FF385C)

9. **Фильтры на странице /tours**
   - Сделать чипсы (chips) вместо кнопок
   - По категориям, локациям, ценам

10. **Календарь: улучшенный функционал**
    - Drag & Drop экскурсий между датами
    - При клике на дату с несколькими экскурсиями - выпадающий список
    - Перетаскивание карточки на другую дату
    - Кнопка "Отказаться от заявки"

11. **Раздел "Заказы" пустой**
    - Детальная информация о транзакциях
    - Кто оплатил, когда, сколько, статус
    - Время экскурсии, контакты клиента

12. **Дашборд гида: добавить "Оборот сделок"**
    - Оборот = полная стоимость всех бронирований
    - Доход = 3% от оборота
    - Показывать обе цифры

### 🟢 УЛУЧШЕНИЯ:

13. **Карточки туров**
    - Доработать дизайн (как у Airbnb)
    - Добавить галерею фото (стрелки влево/вправо)
    - Иконка сердца для избранного
    - Бейдж "Популярное" для часто бронируемых

14. **Алгоритм рекомендаций**
    - "Популярное" - если > 50 бронирований
    - "Хит продаж" - если > 100 бронирований
    - "Часто берут" - если > 5 бронирований за неделю

15. **Наполнить контент**
    - Описания туров (long_description)
    - Журнал: полные статьи с контентом
    - Привязать seed-туры к админу 79177445182

16. **Анимации и плавность**
    - Hover-эффекты на карточках
    - Плавное появление элементов (fade-in)
    - Анимации перехода между страницами
    - Скелетоны при загрузке (shimmer)

17. **Путеводитель для новых пользователей**
    - Tour guide (пошаговые подсказки)
    - Первый вход → показать как создать экскурсию
    - Как взять заявку
    - Как перенести в календаре

---

## 📝 ДЕТАЛЬНЫЙ АНАЛИЗ ПО СТРАНИЦАМ

### Главная страница:

**Что хорошо:**
- ✅ Hero секция с overlay
- ✅ Блок "Планы на осень" (3 карточки)
- ✅ Направления (6 городов)
- ✅ Блок "Как мы делаем"
- ✅ Отзывы (карусель)
- ✅ Подписка на рассылку
- ✅ Статистика (3 цифры)

**Что исправить:**
- ❌ "Популярные экскурсии" - пусто
- ❌ Поиск: слова сливаются (нужны фоны на табах)
- ❌ Кнопка "Искать" с текстом (убрать текст)
- ❌ Кнопка "Заказать экскурсию" - переделать в блок + sticky notification

### Страница /tours:

**Что хорошо:**
- ✅ Sticky поиск
- ✅ Breadcrumbs
- ✅ Сортировка (5 опций)
- ✅ Кнопки фильтров

**Что исправить:**
- ❌ 0 туров (не загружаются)
- ❌ Фильтры - сделать чипсами
- ❌ Нет категорий (чипсы по категориям)

### Журнал:

**Что хорошо:**
- ✅ 6 статей отображаются
- ✅ Фильтры по тегам (#Китай, #Египет)
- ✅ Превью статей

**Что исправить:**
- ⚠️ Наполнить полным контентом (сейчас короткие)

### Личный кабинет (Dashboard):

**Дашборд:**
- ✅ Шапка с ролью (исправлено)
- ✅ Доход (3% комиссия)
- ✅ График доходов
- ✅ Карточки статистики
- ❌ Нет пункта "Админ-панель" (не определяется роль)
- ❌ Нет "Оборота сделок" (добавить)

**Мои экскурсии:**
- ✅ Пусто (системные туры скрыты - отлично!)
- ✅ Кнопка "Создать экскурсию"

**Заявки:**
- ✅ 3 заявки отображаются
- ✅ Фильтры (Все, Короткие, Длинные)
- ✅ Кнопка "Взять заявку"

**Календарь:**
- ✅ Отображение месяца
- ✅ Легенда цветов
- ❌ Нет drag & drop
- ❌ Баг переноса (-1 день)
- ❌ Нет кнопки "Отказаться"

**Заказы:**
- ❌ Пусто (нужен детальный список транзакций)
- ❌ Нет информации о клиентах

---

## 🎯 ПРИОРИТИЗИРОВАННЫЙ ПЛАН

### ФАЗА 1: Исправление критических багов (1-2 часа)

#### 1.1 Исправить загрузку туров на /tours
- Проверить маппинг `toursData?.data?.tours`
- Возможно проблема в новой структуре ответа API
- Добавить логирование для отладки

#### 1.2 Исправить "Популярные экскурсии" на главной
- Аналогично - проверить маппинг данных
- Убедиться что API возвращает туры

#### 1.3 Исправить определение роли ADMIN
- Проверить `/api/v1/users/me` - возвращает ли role
- Проверить useAuthStore - сохраняется ли role
- Показывать пункт "Админ-панель" только для admin

#### 1.4 Исправить баг переноса даты (-1 день)
- Проблема в Date.toISOString() или timezone
- Использовать format(date, 'yyyy-MM-dd') вместо toISOString()

---

### ФАЗА 2: Ребрендинг "Turex Pro" (30 мин)

#### 2.1 Новое название и логотип
- Заменить "ThaiGuide Pro" → "Turex Pro"
- Создать компонент Logo с диагональным градиентом
- Половина черная (#000), половина малиновая (#FF385C)

```tsx
// components/Logo.tsx
export function Logo() {
  return (
    <div className="text-2xl font-bold relative">
      <span className="bg-gradient-to-br from-black via-black to-airbnb-rausch bg-clip-text text-transparent">
        Turex
      </span>
      <span className="text-airbnb-rausch"> Pro</span>
    </div>
  )
}
```

#### 2.2 Обновить все упоминания
- PublicHeader
- PublicFooter
- DashboardLayout
- Мета-теги (title, description)

---

### ФАЗА 3: Улучшение поиска (1 час)

#### 3.1 Табы поиска с фонами
- Убрать слово "НОВОЕ"
- Добавить `bg-white/20 backdrop-blur-sm rounded-full px-4 py-2` на табы
- Текст белый с тенью

#### 3.2 Кнопка поиска
- Убрать слово "Искать"
- Оставить только иконку лупы
- Увеличить размер: `w-14 h-14 bg-airbnb-rausch rounded-full`
- Hover: scale-110 transition

#### 3.3 Функционал поиска (как у Airbnb)
- Автодополнение для "Куда"
- Календарь для "Даты" (диапазон дат)
- Счётчик гостей (взрослые/дети)
- Фильтрация по всем параметрам
- Сохранение в URL query params

---

### ФАЗА 4: Кнопка "Заказать экскурсию" (30 мин)

#### 4.1 Отдельный блок на главной
- Яркий блок с градиентом
- Призыв к действию (CTA)
- Иконка + текст + стрелка

#### 4.2 Sticky notification (плавающая кнопка)
- Позиция: `fixed bottom-6 left-6 z-50`
- Кнопка с иконкой закрытия
- Анимация появления (slide-in-left)
- Сохранение состояния в localStorage ("dismissed")

```tsx
// components/RequestFloatingButton.tsx
export function RequestFloatingButton() {
  const [visible, setVisible] = useState(!localStorage.getItem('request-btn-dismissed'))
  
  return visible ? (
    <motion.div
      initial={{ x: -100, opacity: 0 }}
      animate={{ x: 0, opacity: 1 }}
      className="fixed bottom-6 left-6 z-50"
    >
      <Link to="/request">
        <Button className="bg-airbnb-rausch shadow-2xl">
          ✨ Заказать экскурсию
        </Button>
      </Link>
      <button onClick={() => {
        setVisible(false)
        localStorage.setItem('request-btn-dismissed', 'true')
      }}>
        ✕
      </button>
    </motion.div>
  ) : null
}
```

---

### ФАЗА 5: Фильтры-чипсы на /tours (1 час)

#### 5.1 Категории чипсами
- Горизонтальная прокрутка
- Активный чип: `bg-airbnb-rausch text-white`
- Неактивный: `bg-gray-100 text-gray-700`
- С количеством: "История (15)"

#### 5.2 Быстрые фильтры
- Цена: "До 5000₽", "5000-10000₽", "10000+₽"
- Длительность: "1-3ч", "4-6ч", "7+ч"
- Рейтинг: "4.5+", "4.7+", "4.9+"

---

### ФАЗА 6: Календарь с Drag & Drop (2-3 часа)

#### 6.1 Библиотека
- Использовать `@dnd-kit/core` для drag and drop
- Или `react-beautiful-dnd`

#### 6.2 Функционал
- Зажать карточку экскурсии → появляется ghost (полупрозрачная копия)
- Перетащить на другую дату
- Drop → API PUT /requests/{id}/reschedule
- Если несколько на одной дате:
  - Клик → выпадает список карточек
  - Выбрать нужную → перетащить

#### 6.3 Кнопка "Отказаться"
- На каждой карточке экскурсии в календаре
- Подтверждение: "Вы уверены?"
- DELETE /requests/{id} или PUT status=cancelled
- Освобождает часы

---

### ФАЗА 7: Раздел "Заказы" (1-2 часа)

#### 7.1 Таблица транзакций
Колонки:
- ID заказа
- Экскурсия (название)
- Клиент (имя, телефон, email)
- Дата экскурсии
- Время
- Участники
- Стоимость (полная)
- Ваш доход (3%)
- Статус оплаты (Оплачено/Ожидает)
- Дата бронирования

#### 7.2 Фильтры
- По статусу (все, оплачено, ожидает, отменено)
- По дате (сегодня, неделя, месяц, все)
- Поиск по имени клиента или ID

#### 7.3 Детали заказа (модальное окно)
- Все данные клиента
- История статусов
- Кнопка "Отменить бронирование"
- Кнопка "Связаться с клиентом" (WhatsApp/Telegram)

---

### ФАЗА 8: Дашборд - оборот сделок (30 мин)

#### 8.1 Добавить карточку "Оборот"
```tsx
<Card>
  <CardHeader>
    <CardTitle>Оборот сделок</CardTitle>
  </CardHeader>
  <CardContent>
    <p className="text-4xl font-bold text-blue-600">
      {formatRUB(totalRevenue)}  {/* Полная стоимость */}
    </p>
    <p className="text-sm text-gray-500">
      Ваш доход: {formatRUB(totalRevenue * 0.03)}
    </p>
  </CardContent>
</Card>
```

#### 8.2 График оборота vs дохода
- Две линии на графике
- Синяя: оборот (100%)
- Малиновая: доход (3%)

---

### ФАЗА 9: Карточки туров (1 час)

#### 9.1 Галерея фото
- Стрелки влево/вправо
- Индикаторы (точки) снизу
- Hover на фото → показать стрелки

#### 9.2 Иконка сердца (избранное)
- Правый верхний угол
- Сохранение в localStorage
- Анимация при клике (scale + fill)

#### 9.3 Бейджи
- "🔥 Популярное" - если bookings_count > 50
- "⭐ Хит продаж" - если bookings_count > 100
- "🆕 Новое" - если created_at < 7 дней назад
- "💰 Скидка -X%" - если has_discount

---

### ФАЗА 10: Наполнение контента (1-2 часа)

#### 10.1 Обновить seed-туры
- Привязать к admin (ID: первый ADMIN в системе, а не системный гид)
- Заполнить long_description (500+ символов)
- Добавить what_to_expect (детально)
- Добавить included/not_included (списки)
- Добавить organizational_details

#### 10.2 Журнал
- Расширить контент статей до 2000+ символов
- Добавить подзаголовки (h2, h3)
- Добавить списки и цитаты
- Добавить "Связанные туры" в конце статьи

#### 10.3 Связь туров с админом
- В карточке тура показывать гида
- Для seed-туров: "Гид: Администратор (Turex Pro)"

---

### ФАЗА 11: Анимации и эффекты (1 час)

#### 11.1 Hover-эффекты
- Карточки туров: поднятие + тень
- Кнопки: масштаб 105%
- Иконки: rotate или bounce

#### 11.2 Fade-in анимации
- При скролле: элементы появляются снизу
- Использовать Framer Motion variants

#### 11.3 Skeleton loaders
- При загрузке туров - shimmer skeleton
- При загрузке дашборда - skeleton cards

#### 11.4 Переходы между страницами
- Fade transition с Framer Motion

---

### ФАЗА 12: Путеводитель (30 мин - опционально)

#### 12.1 Библиотека
- `react-joyride` или `driver.js`

#### 12.2 Туры для гида
1. "Создайте свою первую экскурсию"
2. "Посмотрите доступные заявки"
3. "Управляйте расписанием в календаре"
4. "Отслеживайте заказы"

---

## 🛠 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Исправление маппинга данных:

**Проблема:** `toursData?.data?.tours` возвращает undefined

**Решение:**
```typescript
// Проверить структуру ответа API
const { data } = useQuery({
  queryFn: () => api.get('/tours/').then(res => res.data)
})

// Вариант 1: res.data.tours
const tours = data?.tours || []

// Вариант 2: res.data.data.tours
const tours = data?.data?.tours || []

// Добавить console.log для отладки
console.log('Tours API response:', data)
```

### Исправление бага календаря (-1 день):

**Проблема:** Date timezone offset

**Решение:**
```typescript
// Неправильно:
const dateString = date.toISOString().split('T')[0]
// Может дать 2025-10-22 вместо 2025-10-23 из-за UTC

// Правильно:
import { format } from 'date-fns'
const dateString = format(date, 'yyyy-MM-dd')
```

### Drag & Drop для календаря:

```tsx
import { DndContext, useDraggable, useDroppable } from '@dnd-kit/core'

function DraggableRequest({ request }) {
  const { attributes, listeners, setNodeRef } = useDraggable({
    id: request.id,
    data: request
  })
  
  return (
    <div ref={setNodeRef} {...listeners} {...attributes}>
      {/* Карточка экскурсии */}
    </div>
  )
}

function DroppableDate({ date, children }) {
  const { setNodeRef } = useDroppable({ id: date.toISOString() })
  
  return (
    <div ref={setNodeRef}>
      {children}
    </div>
  )
}
```

---

## 📊 ИТОГОВАЯ СТАТИСТИКА

### Проверено:
- 🌐 5 публичных страниц
- 🔐 6 страниц личного кабинета
- 🎨 Дизайн и UX
- 🔧 Функционал

### Найдено проблем:
- 🔴 Критических: 4
- 🟡 Важных: 8
- 🟢 Улучшений: 5
- **Итого:** 17 проблем/задач

### Время выполнения:
- Фаза 1: 1-2 часа
- Фаза 2: 30 минут
- Фаза 3: 1 час
- Фаза 4: 30 минут
- Фаза 5: 1 час
- Фаза 6: 2-3 часа
- Фаза 7: 1-2 часа
- Фаза 8: 30 минут
- Фаза 9: 1 час
- Фаза 10: 1-2 часа
- Фаза 11: 1 час
- Фаза 12: 30 минут (опционально)

**Общее время:** 10-15 часов работы

---

## 🎨 ВИЗУАЛЬНЫЕ ИЗМЕНЕНИЯ

### Убрать градиенты, оставить только малиновый:

**Текущее (градиенты):**
```css
bg-gradient-to-r from-tropical-ocean to-tropical-turquoise
bg-gradient-to-b from-black/60 via-black/40 to-black/70
```

**Новое (только малиновый):**
```css
bg-airbnb-rausch
bg-airbnb-rausch/90  /* hover */
bg-airbnb-rausch/10  /* light backgrounds */
```

### Hero фото:
- Убрать градиент overlay
- Оставить только `bg-airbnb-rausch/60` (однотонный малиновый)
- Или вообще без overlay, но с качественным фото

---

## 🚀 ГОТОВО К РЕАЛИЗАЦИИ

**Все проблемы задокументированы.**  
**План составлен.**  
**Приоритеты расставлены.**

**Начинаем с Фазы 1?**

